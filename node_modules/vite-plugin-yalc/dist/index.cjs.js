"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const node_child_process = require("node:child_process");
const runCommand = (command) => {
  return {
    name: "run-command",
    closeBundle: async () => {
      return node_child_process.exec(command, (response, error) => {
        if (error) console.error(error);
        if (response) console.log(response);
        return node_child_process.exec("npx yalc push", (response2, error2) => {
          if (error2) console.error(error2);
          if (response2) console.log(response2);
        });
      });
    }
  };
};
const runYalc = () => {
  return {
    name: "run-yalc",
    closeBundle: async () => {
      return node_child_process.exec("npx yalc push", (response, error) => {
        if (error) console.error(error);
        if (response) console.log(response);
      });
    }
  };
};
exports.runCommand = runCommand;
exports.runYalc = runYalc;
